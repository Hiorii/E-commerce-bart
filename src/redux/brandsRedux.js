/* selectors */
export const getAllBrands = ({ brands }) => brands;

/* reducer */
export default function reducer(statePart = [], action = {}) {
  switch (action.type) {
    default:
      return statePart;
  }
}
